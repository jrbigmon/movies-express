<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title class="titulo">Editar Filme</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <figure>
        <a  class="botaoVoltar" href="/movies"><img src="/img/logo-DH.png" alt="Logo Digital House"> Voltar a Lista de Filmes</a></p>
      </figure>
    <h1 class="titulo">Filme a Editar:  <%= movie.title %></h1>
    <section class="formulario">
        <form action="/movies/update/<%= movie.id %>?_method=PUT" method="POST">
            <label for="title">Title </label>
            <p><input type="text" name="title" id="title" value="<%= movie.title %>" required></p>
            
            <label for="rating">Rating </label>
            <p><input type="number" name="rating" id="rating" value="<%= movie.rating %>" required></p>
           
            <label for="awards">Awards </label>
            <p><input type="number" name="awards" id="awards" value="<%= movie.awards %>" required></p>
            
            <label for="release_date">Release date: <br> <%= movie.release_date.toLocaleDateString('pt-BR') %></label>
            <p><input type="date" name="release_date" id="release_date"></p>
            
            <label for="length">Length </label>
            <p><input type="number" name="length" id="" value="<%= movie.length %>" required></p>
            
            <label for="genre">Genero:</label> <br>
            <select name="genre_id" id="genre">
                <% genres.forEach(genre => { %>

                <option value="<%= genre.id %>" <%= genre.id == movie.genre_id ? 'selected' : ''%> > <%= genre.name %> </option>
                
                <% }) %> 
            </select>

            <br><br>
            <p> <button class="botaoAtualizar" type="submit">Atualizar</button>
        
        </form>
    </section>
</body>

</html>